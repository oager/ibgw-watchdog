# Shell scripts and systemd units must use LF — CRLF breaks the bash shebang
# and systemd parsing when cloned on Linux.
*.sh            text eol=lf
*.service       text eol=lf
install.sh      text eol=lf
*.conf.example  text eol=lf
